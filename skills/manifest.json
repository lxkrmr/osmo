{
  "odoo-otto": {
    "requirements": {
      "commands": [
        "otto"
      ]
    },
    "note": "Requires the global otto command and completed otto setup."
  },
  "odoo-log-debug": {
    "requirements": {
      "project_files": [
        "docker-compose.yml"
      ]
    },
    "note": "Uses docker compose logs for local Odoo diagnostics."
  }
}
